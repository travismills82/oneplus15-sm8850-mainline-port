# One-shot legacy branch cleanup

This temporary maintenance branch exists only to run the observable GitHub Actions audit that removes verified Linux 6.12.23 branch refs after preserving the final historical snapshot as the immutable tag `archive/canoe-6.12.23-final`.

The active Linux 7.1.1 branches are explicitly protected by the workflow.
